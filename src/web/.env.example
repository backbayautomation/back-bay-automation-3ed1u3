# API Configuration
# Required: Base URL for backend API endpoints
# Validation: Must be a valid URL with appropriate protocol (http/https)
# Security: Must use HTTPS in production environment
VITE_API_BASE_URL=http://localhost:8000/api/v1

# API Request Timeout (milliseconds)
# Optional: Configurable timeout for API requests
# Validation: Must be a positive integer between 1000 and 60000
# Performance: Adjust based on network conditions and API response times
VITE_API_TIMEOUT=30000

# Application Branding
# Required: Application title for browser and UI display
# Validation: Non-empty string without special characters
# Branding: Should match client branding requirements
VITE_APP_TITLE=AI-Powered Product Catalog Search

# WebSocket Configuration
# Required: WebSocket server URL for real-time features
# Validation: Must be a valid WebSocket URL (ws/wss)
# Security: Must use WSS in production environment
VITE_WEBSOCKET_URL=ws://localhost:8000/ws

# Authentication Configuration
# Required: Local storage key for auth token
# Validation: Alphanumeric string without spaces
# Security: Should be unique and non-predictable in production
VITE_AUTH_STORAGE_KEY=app_auth_token

# Upload Configuration
# Optional: Maximum file size for document uploads (in bytes)
# Validation: Positive integer, maximum 52428800 (50MB)
# Performance: Adjust based on server capacity and network conditions
VITE_MAX_FILE_SIZE=10485760

# Feature Flags
# Optional: Toggle for analytics features
# Validation: Boolean value (true/false)
# Privacy: Must comply with data protection regulations
VITE_ENABLE_ANALYTICS=true

# Logging Configuration
# Optional: Application logging level
# Validation: One of: debug, info, warn, error
# Security: Set to warn or error in production to minimize exposure
VITE_LOG_LEVEL=info

# Production Environment Example Values:
# VITE_API_BASE_URL=https://api.example.com/v1
# VITE_WEBSOCKET_URL=wss://api.example.com/ws
# VITE_LOG_LEVEL=warn
# VITE_ENABLE_ANALYTICS=true